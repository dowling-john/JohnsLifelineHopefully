[project]
name = "johnslifelinehopefully"
version = "0.1.0"
description = ""
authors = [
    {name = "john dowling",email = "john.dowling@diskarchive.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests (>=2.34.2,<3.0.0)",
    "websocket-client (>=1.9.2,<2.0.0)",
    "pytest (>=9.1.1,<10.0.0)",
    "tensorflow (>=2.21.0,<3.0.0)",
    "uvicorn (>=0.53.0,<0.54.0)",
    "pandas (>=3.0.5,<4.0.0)",
    "fastapi (>=0.141.1,<0.142.0)",
    "yfinance (>=1.7.0,<2.0.0)",
    "websockets (>=17.1,<18.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
